{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "slovplantlist-admin:start": "yarn --cwd packages/slovplantlist-admin-js start",
    "slovplantlist-web:start": "yarn --cwd packages/slovplantlist-web-app start"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
